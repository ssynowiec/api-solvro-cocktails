services:
  postgres:
    image: postgres:latest
    restart: always
    environment:
      POSTGRES_DB: api-cocktails-solvro
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
